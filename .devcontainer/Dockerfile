# Development container. Deliberately NOT pinned, and this comment is the
# exception the image policy requires (CONTRIBUTING.md, "Image pinning").
#
# Nothing here is deployed: this file builds a contributor's editor container,
# and the stack it works on runs from docker-compose.*.yml, whose images ARE
# pinned and are asserted by tests/compose-validation.bats. Pinning it would mean
# chasing `npm@latest` and the Claude Code package on every release for no
# runtime benefit. It floats on purpose, and a bad upstream release is a broken
# devcontainer, not a broken NAS.
FROM mcr.microsoft.com/devcontainers/base:ubuntu

# Node.js (for Claude Code + Playwright).
# `--no-install-recommends` matches every other install in this file. Trivy's
# DS-0029 caught its absence here: without it the NodeSource script drags in the
# recommended set, which this container never uses.
RUN curl -fsSL https://deb.nodesource.com/setup_22.x | bash - \
    && apt-get install -y --no-install-recommends nodejs \
    && rm -rf /var/lib/apt/lists/*

# Docker CLI + Compose plugin (to manage the project's docker-compose stack)
RUN apt-get update && apt-get install -y --no-install-recommends \
      ca-certificates curl gnupg \
    && install -m 0755 -d /etc/apt/keyrings \
    && curl -fsSL https://download.docker.com/linux/ubuntu/gpg \
       | gpg --dearmor -o /etc/apt/keyrings/docker.gpg \
    && chmod a+r /etc/apt/keyrings/docker.gpg \
    && echo "deb [arch=$(dpkg --print-architecture) signed-by=/etc/apt/keyrings/docker.gpg] \
       https://download.docker.com/linux/ubuntu $(. /etc/os-release && echo $VERSION_CODENAME) stable" \
       > /etc/apt/sources.list.d/docker.list \
    && apt-get update && apt-get install -y --no-install-recommends \
       docker-ce-cli docker-compose-plugin \
    && rm -rf /var/lib/apt/lists/*

# Python3 + PyYAML (for pre-commit YAML validation and fix-radarr-paths.sh)
RUN apt-get update && apt-get install -y --no-install-recommends \
      python3 python3-pip \
    && pip3 install --break-system-packages pyyaml \
    && rm -rf /var/lib/apt/lists/*

# GitHub CLI
RUN apt-get update && apt-get install -y --no-install-recommends gh \
    && rm -rf /var/lib/apt/lists/*

# sshpass (for NAS SSH access from scripts)
# dnsutils provides dig (for pre-commit domain checks)
RUN apt-get update && apt-get install -y --no-install-recommends \
      sshpass dnsutils \
    && rm -rf /var/lib/apt/lists/*

# Claude Code CLI — install as vscode user to avoid npm permission errors
USER vscode
RUN mkdir -p /home/vscode/.npm-global \
    && npm config set prefix /home/vscode/.npm-global \
    && npm install -g npm@latest \
    && npm install -g @anthropic-ai/claude-code
ENV PATH="/home/vscode/.npm-global/bin:${PATH}"
USER root

# Playwright system deps (for running e2e tests)
RUN npx playwright install-deps chromium 2>/dev/null || true
