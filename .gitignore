# Environment & Secrets
.env
.env.e2e
.env.nas.backup
*.key
*.pem
*.crt

# Private Configuration (see .claude/config.local.md.example)
.claude/config.local.md
.claude/settings.local.json

# Local-only client-side notes (kept out of git)
.claude/playback-diagnostics.md

# Traefik SSL Certificates
traefik/acme.json
traefik/*.json
traefik/certs/

# Traefik Config Files (copy from .example and customize)
traefik/traefik.yml
traefik/dynamic/vpn-services.yml
traefik/dynamic/*.local.yml

# Pi-hole Local DNS (copy from .example and customize)
pihole/dnsmasq.d/02-local-dns.conf

# Homepage dashboard - runtime-generated logs (config YAML is tracked)
homepage/config/logs/

# Configarr (copy from .example and customize)
configarr/config.yml

# Cloudflared Credentials (locally-managed tunnel)
cloudflared/config.yml
cloudflared/credentials.json
cloudflared/cert.pem

# Application Configs (these should be on NAS, not in repo)
gluetun-config/
seerr/
bazarr/
notifiarr/
homarr/

# Backup Files
*.tar.gz
*.zip
*.backup
backups/

# Logs
*.log
logs/

# OS Files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db
desktop.ini

# IDE / Editor
.vscode/
.idea/
*.swp
*.swo
*~
.project
.classpath
.settings/
*.sublime-project
*.sublime-workspace
*.code-workspace

# Docker
docker-compose.override.yml
.docker/

# Temporary Files
*.tmp
*.temp
tmp/
temp/

# Node (if you add any scripts)
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
package-lock.json
yarn.lock

# Python (if you add any scripts)
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
venv/
env/
ENV/

# Personal Notes & Local Docs
notes.md
TODO.md
scratch/
docs/RAM-UPGRADE-BASELINE.md
docs/*-BASELINE*.md

# E2E test artifacts
tests/e2e/screenshots/*.png
test-results/

# Camera Listen holds the NVR password
camera-listen/.env

# Terraform state (contains resolved API key values, not encrypted at rest)
# .terraform.lock.hcl IS committed - it pins provider versions, no secrets in it
terraform/.terraform/
terraform/terraform.tfstate
terraform/terraform.tfstate.backup
terraform/*.tfvars
tests/mutation/.mutants/
